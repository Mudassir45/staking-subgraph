{
  "name": "lp-staking2",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ mudassir45/lp-staking2",
    "create-local": "graph create --node http://localhost:8020/ mudassir45/lp-staking2",
    "remove-local": "graph remove --node http://localhost:8020/ mudassir45/lp-staking2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mudassir45/lp-staking2"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
